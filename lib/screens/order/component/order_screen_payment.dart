import 'package:flutter/material.dart';

class OrderScreenPayment extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Container(height: 10,color: Colors.black12),
        Text("카드",style: TextStyle(fontSize: 100)),
      ],
    );
  }
}
