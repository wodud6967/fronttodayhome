import 'package:flutter/material.dart';

import 'component/cart_screen_body.dart';

class CartScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: CartScreenBody(),
    );
  }
}
