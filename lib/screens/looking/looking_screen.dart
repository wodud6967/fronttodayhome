import 'package:flutter/material.dart';
class LookingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('LookingScreen'),
    );
  }
}